# Secrets ONLY — every non-secret deployment parameter lives in
# scripts/deploy/config.ts (git-reviewed).
DEPLOYER_PRIVATE_KEY=
ETHERSCAN_API_KEY=
# Blockscout account API key: meters the central gateway used for status
# reads; it does NOT raise the instance's own per-IP submission limit.
BLOCKSCOUT_API_KEY=
# Optional keyed RPC endpoint overrides (public defaults in hardhat.config.ts)
#ROBINHOOD_RPC_URL=
#ROBINHOOD_TESTNET_RPC_URL=
